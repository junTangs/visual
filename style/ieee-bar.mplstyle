# Matplotlib style for IEEE plots
# This style should work for most two-column journals

# Set color cycle
# Set line style as well for black and white graphs
axes.prop_cycle : (cycler('color', ['k', 'r', 'b', 'g','FFFF07']) + cycler('ls', ['-', '--', ':', '-.','-']) + cycler('marker',['.',',','o','v','^']))

# Set default figure size
figure.figsize : 3.3, 2.5
figure.dpi : 600

# Font sizes
font.size : 20
font.family : serif
font.serif : Times
